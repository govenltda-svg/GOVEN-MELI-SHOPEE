{
  "name": "goven",
  "version": "1.0.0",
  "description": "Dashboard financeiro e logístico Goven — ML + Shopee + Amazon",
  "private": true,
  "scripts": {
    "dev":   "vercel dev",
    "build": "echo 'static site — no build needed'",
    "lint":  "eslint api/**/*.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
